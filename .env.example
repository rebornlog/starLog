# ⚙️ starLog 环境变量配置

> 复制此文件为 `.env.local` 并填入实际值

---

## 📋 快速配置（最小化）

```bash
# 数据库连接（必需）
DATABASE_URL="postgresql://用户名：密码@localhost:5432/starlog"

# 示例：
# DATABASE_URL="postgresql://postgres:123456@localhost:5432/starlog"
```

---

## 🔧 完整配置

### ============ 数据库配置 ============

```bash
# PostgreSQL 连接字符串（必需）
# 格式：postgresql://用户名：密码@主机：端口/数据库名
DATABASE_URL="postgresql://starlog:starlog123@localhost:5432/starlog?schema=public"
```

**如何获取连接字符串**:
```bash
# 本地 PostgreSQL
# 默认：postgresql://postgres:@localhost:5432/postgres

# 创建数据库后：
# postgresql://你的用户名：你的密码@localhost:5432/starlog
```

---

### ============ Redis 配置 ============

```bash
# Redis 连接字符串（可选，用于缓存）
# 不配置则直接使用数据库查询
REDIS_URL="redis://localhost:6379"

# 远程 Redis
# REDIS_URL="redis://用户名：密码@host:port"
```

---

### ============ 应用配置 ============

```bash
# 运行环境
NODE_ENV="development"  # 生产环境改为 production

# 应用端口
PORT=3000

# 站点 URL（生产环境修改）
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# 站点名称
NEXT_PUBLIC_SITE_NAME="starLog"
```

---

### ============ 金融服务配置 ============

```bash
# Python 金融服务地址（可选）
# 如果没有运行金融服务，可以留空
FINANCE_SERVICE_URL="http://localhost:8000"
```

---

### ============ 认证配置 ============

```bash
# NextAuth 用户认证（可选）
# 生成密钥：openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"
```

---

### ============ 文件上传配置 ============

```bash
# 最大上传文件大小（字节）
# 10MB = 10485760
MAX_FILE_SIZE=10485760

# 允许上传的文件类型
ALLOWED_FILE_TYPES="application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,text/markdown,text/plain,image/jpeg,image/png"
```

---

## 📝 配置说明

### 必需配置

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `DATABASE_URL` | PostgreSQL 连接字符串 | `postgresql://user:pass@localhost:5432/db` |

### 可选配置

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| `REDIS_URL` | Redis 连接字符串 | 无（不使用缓存） |
| `PORT` | 应用端口 | `3000` |
| `NEXT_PUBLIC_SITE_URL` | 站点 URL | `http://localhost:3000` |
| `FINANCE_SERVICE_URL` | 金融服务地址 | 无 |

---

## 🔑 数据库连接字符串格式

```
postgresql://用户名：密码@主机：端口/数据库名?schema=public
```

**示例**:
```bash
# 本地数据库
DATABASE_URL="postgresql://postgres:123456@localhost:5432/starlog"

# 远程数据库
DATABASE_URL="postgresql://user:password@192.168.1.100:5432/starlog"

# 云服务器（阿里云 RDS）
DATABASE_URL="postgresql://user:password@rm-xxx.pg.rds.aliyuncs.com:3432/starlog"
```

---

## 🔐 生成 NEXTAUTH_SECRET

```bash
# Mac/Linux
openssl rand -base64 32

# Windows (PowerShell)
[Convert]::ToBase64String((New-Object Security.Cryptography.RNGCryptoServiceProvider).GetBytes(32))
```

---

## ✅ 验证配置

```bash
# 检查 .env.local 文件是否存在
ls -la .env.local

# 检查环境变量是否加载
npm run dev

# 查看是否有配置错误
# 如果有错误，控制台会显示
```

---

## 🐛 常见问题

### 1. DATABASE_URL 格式错误

**错误信息**: `Invalid DATABASE_URL`

**解决方法**:
```bash
# 确保格式正确
DATABASE_URL="postgresql://user:password@localhost:5432/database"

# 检查特殊字符是否需要 URL 编码
# 例如：@ 需要编码为 %40
```

### 2. 数据库连接失败

**错误信息**: `ECONNREFUSED`

**解决方法**:
```bash
# 检查 PostgreSQL 是否运行
sudo systemctl status postgresql

# 检查端口是否开放
lsof -i :5432

# 检查防火墙
sudo ufw status
```

### 3. Redis 连接失败

**错误信息**: `Redis connection failed`

**解决方法**:
```bash
# Redis 是可选的，可以注释掉 REDIS_URL
# REDIS_URL="redis://localhost:6379"

# 或使用 Redis
sudo systemctl start redis
```

---

## 📞 获取帮助

- 📖 完整文档：[DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
- 🐛 Issue: [GitHub Issues](https://github.com/rebornlog/starLog/issues)
- 📧 Email: 944183654@qq.com

---

**配置完成后，运行 `npm run dev` 启动项目！** 🚀
